name: Conda Build and Upload

on:
  release:
    types: [published]
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      upload:
        description: 'Upload to Anaconda Cloud'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

permissions: read-all

jobs:
  conda-build:
    runs-on: ubuntu-latest
    name: Build conda package (linux-64)

    defaults:
      run:
        shell: bash -l {0}

    steps:
      - uses: actions/checkout@v6

      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: '3.11'
          channels: conda-forge,bioconda,defaults
          channel-priority: strict

      - name: Install conda-build and anaconda-client
        # Install into the base env: `conda build` is a plugin of the conda
        # driver (which lives in base), so installing it into the activated
        # `test` env leaves `conda build` an "invalid choice". conda-verify is
        # dropped - it has no build for base's python 3.13 and is an optional
        # package linter, not required by `conda build`.
        run: |
          conda install -n base -y -c conda-forge conda-build anaconda-client

      - name: Extract version from DESCRIPTION
        id: version
        run: |
          VERSION=$(grep '^Version:' DESCRIPTION | sed 's/Version: //')
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Package version: ${VERSION}"

      - name: Build conda package
        run: |
          conda build conda-recipe/ \
            --channel conda-forge \
            --channel bioconda \
            --no-anaconda-upload \
            --output-folder conda-output/
        env:
          GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload conda package to Anaconda Cloud
        if: >-
          (github.event_name == 'release') ||
          (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')) ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.upload == 'true')
        run: |
          conda run -n base anaconda -t "$ANACONDA_TOKEN" upload \
            --user aviezerl \
            --force \
            conda-output/linux-64/r-dafrjulia-*.tar.bz2
        env:
          ANACONDA_TOKEN: ${{ secrets.ANACONDA_TOKEN }}

      - name: Upload build artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: conda-package-linux-64
          path: conda-output/linux-64/r-dafrjulia-*.tar.bz2
          if-no-files-found: warn
